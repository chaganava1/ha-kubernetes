---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
platforms:

  - name: controller-1
    image: "geerlingguy/docker-centos7-ansible:latest"
    command: "/sbin/init"
    cgroupns_mode: host
    #volumes:
    #  - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: true
    pre_build_image: true
    groups:
      - controllers
    network_mode: host
    #networks:
    #  - name: molecule_network

  - name: controller-2
    image: "geerlingguy/docker-centos7-ansible:latest"
    command: "/sbin/init"
    cgroupns_mode: host
    #volumes:
    #  - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: true
    pre_build_image: true
    groups:
      - controllers
    network_mode: host
    #networks:
    #  - name: molecule_network

  - name: controller-3
    image: "geerlingguy/docker-centos7-ansible:latest"
    command: "/sbin/init"
    cgroupns_mode: host  
    #volumes:
    #  - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: true
    pre_build_image: true
    groups:
      - controllers
    network_mode: host
    #networks:
    #  - name: molecule_network

  - name: worker-1
    image: "geerlingguy/docker-centos7-ansible:latest"
    command: "/sbin/init"
    cgroupns_mode: host   
    #volumes:
    #  - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: true
    pre_build_image: true
    groups:
      - workers
    network_mode: host
    #networks:
    #  - name: molecule_network

provisioner:
  name: ansible
  Playbooks:
    converge: converge.yml
  inventory:
    host_vars:
      controller-1:
        node_ip: "192.168.60.101"
      controller-2:
        node_ip: "192.168.60.102"
      controller-3:
        node_ip: "192.168.60.103"
      worker-1:
        node_ip: "192.168.60.111"
verifier:
  name: ansible
